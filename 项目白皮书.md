# 极简记账 APP 项目技术白皮书.md

**文档版本**：v1.0.0
**撰写日期**：2026年8月
**目标读者**：前端工程师、后端工程师、UI/UX设计师、QA及项目干系人

---

## 1. 项目概述 (Project Overview)

本项目旨在开发一款类似“钱迹”的纯粹、无广告、无多余社交功能的极简记账应用。核心诉求在于**“快速记录”**与**“数据资产完全掌控”**。
为了最大限度降低运维成本并实现全平台覆盖，本项目将采用**“一套前端代码跨端打包”**的策略，兼容 Android、iOS、PC（Windows/macOS）以及 Web 端。后端基础设施全面拥抱 **Cloudflare Serverless 生态**。

## 2. 产品设计与 UI/UX 规范 (Design & UI/UX)

*   **核心设计理念**：极简、克制、高效。
*   **视觉风格**：大面积留白，采用低饱和度的莫兰迪色系或极简黑白灰，配合流畅的微动效。支持全局暗黑模式（Dark Mode）。
*   **交互原则**：
    *   **3秒记账**：打开APP即为记账页面，减少操作层级。
    *   **直觉操作**：通过滑动切换支出/收入，通过清晰的Icon快速定位分类。
    *   **无感同步**：弱网或无网环境下依然可以流畅记账，网络恢复后后台静默同步。

## 3. 功能架构 (Functional Architecture)

产品功能划分为以下五个核心模块：

### 3.1 核心记账模块 (Transaction Module)
*   **四种账单类型**：
    *   `支出 (Expense)`：日常消费。
    *   `收入 (Income)`：工资、理财收益等。
    *   `转账 (Transfer)`：个人不同资产账户之间的资金互转。
    *   `借贷 (Loan)`：借入、借出、还款、收款。
*   **记录要素**：金额、分类、账本、日期、备注、图片附件（可选）。

### 3.2 分类管理模块 (Category Module)
*   **层级架构**：支持“大分类”+“小分类”的二级联动（例：大分类[餐饮] -> 小分类[早餐、午餐、晚餐、夜宵]）。
*   **自定义能力**：用户可自由添加、删除、修改、排序大/小分类，支持自定义分类Icon和颜色。

### 3.3 账本管理模块 (Ledger Module)
*   **多账本支持**：用户可创建多个账本（如：日常账本、装修账本、旅游账本、家庭账本），不同账本数据物理/逻辑隔离，报表独立。

### 3.4 预算管理模块 (Budget Module)
*   **预算设置**：支持设置“总预算”及“大分类预算”（如餐饮预算3000元，住宿预算2000元）。
*   **预警机制**：预算消耗达到80%或超支时，UI上给予明显的颜色预警提示。

### 3.5 数据与资产模块 (Data Management)
*   **数据导入**：支持解析主流格式的CSV账单文件（如支付宝、微信、旧版记账软件导出文件）导入本系统。
*   **数据导出**：支持一键将某账本或全量数据导出为 CSV/JSON 格式，保障用户的数据所有权。

---

## 4. 技术架构选型 (Technical Architecture)

为了满足“一套代码兼容安卓、苹果、PC”且后端使用 Cloudflare 生态的要求，制定如下技术栈：

### 4.1 前端跨端方案
*   **核心框架**： `React 18` + `Vite` + `Tailwind CSS` (保证轻量和现代化开发体验)
*   **跨平台打包方案**：
    *   **Web 端**：直接部署为 PWA（渐进式 Web 应用），托管于 **Cloudflare Pages**。
    *   **移动端 (iOS/Android)**：使用 **Tauri Mobile** 将 Web 代码打包为原生 App。
    *   **PC 端 (Windows/macOS)**：使用 **Tauri** 进行封装。
*   **状态与本地存储**：由于记账软件高度依赖“离线可用性”，前端需集成 `IndexedDB` (使用 Dexie.js)，实现**离线优先 (Offline-first)**。

### 4.2 后端与云服务方案 (Cloudflare 生态)
*   **API 托管**：**Cloudflare Workers** (使用 `Hono.js` 框架)。
*   **数据库**：**Cloudflare D1** (构建在 SQLite 之上的 Serverless 数据库，完美契合结构化记账数据，延迟低，成本极低)。
*   **静态资源**：**Cloudflare Pages** (用于托管前端产物)。
*   **对象存储**：**Cloudflare R2** (用于存储用户上传的账单小票图片、用户头像等)。

---

## 5. 核心数据库设计 (D1 Database Schema)

Cloudflare D1 底层为 SQLite，设计应尽量遵循关系型数据库范式，同时考虑查询效率。

### 5.1 核心表结构预览

1.  **Users 表 (用户表)**
    *   `user_id` (PK)
    *   `email`, `password_hash`, `created_at`, `updated_at`
2.  **Ledgers 表 (账本表)**
    *   `ledger_id` (PK)
    *   `user_id` (FK)
    *   `name` (账本名称), `currency` (币种), `is_default` (是否默认)
3.  **Categories 表 (分类表)**
    *   `category_id` (PK)
    *   `user_id` (FK) - 区分不同用户的自定义分类
    *   `type` (枚举: expense, income)
    *   `parent_id` (自关联外键，NULL为大分类，有值为小分类)
    *   `name` (分类名称), `icon` (图标标识), `sort_order` (排序)
4.  **Transactions 表 (账单流水表 - 核心表)**
    *   `transaction_id` (PK)
    *   `user_id` (FK), `ledger_id` (FK)
    *   `type` (枚举: expense, income, transfer, loan)
    *   `amount` (DECIMAL/Integer, 建议存分为单位，避免浮点精度丢失)
    *   `category_id` (FK)
    *   `from_account`, `to_account` (用于转账和借贷场景)
    *   `transaction_date` (记账日期), `remark` (备注)
    *   `sync_status` (同步状态，用于离线同步逻辑)
5.  **Budgets 表 (预算表)**
    *   `budget_id` (PK)
    *   `user_id` (FK), `ledger_id` (FK)
    *   `category_id` (FK，NULL表示该账本总预算，有值表示大分类预算)
    *   `period` (枚举: monthly, yearly)
    *   `amount` (预算金额)

---

## 6. 核心技术难点与开发规范 (Key Challenges & Specs)

### 6.1 离线优先与数据同步冲突处理 (Offline-First & Sync)
*   **痛点**：记账 APP 必须做到秒开且无网可记。
*   **策略**：前端采用 `UUID v4` 生成主键。用户操作全部直接写入本地 `IndexedDB`。网络畅通时，后台 Worker 轮询或基于事件推送到 Cloudflare Workers API。
*   **冲突解决**：采用 `Last-Write-Wins` (最后修改者胜出) 策略，配合 `updated_at` 时间戳进行版本控制。

### 6.2 金额精度问题 (Precision)
*   **规范**：坚决禁止使用 `Float` 或 `Double` 存储金额。前后端交互及 D1 数据库存储一律将金额**放大100倍以整数 (Integer) 存储**（例如：12.34元 存为 1234）。前端展示时再除以100。

### 6.3 性能与 Cloudflare 限制
*   **Workers 限制**：注意单个请求的 CPU 时间限制。批量导入导出数据（如解析大规模 CSV）时，建议在前端/客户端完成解析和数据转换，将转换后的标准 JSON 分批次发送给 Workers 写入 D1，减轻 Edge 端的计算压力。

---

## 7. 项目实施路径与里程碑 (Milestones)

项目按敏捷开发模式分为三个阶段 (Phases)：

### Phase 1: MVP（最小可行性产品）
*   搭建 前端 + Capacitor/Tauri + CF Workers + D1 的开发管线。
*   实现用户注册/登录 (JWT 认证)。
*   实现单账本下的“支出/收入”记录、查看明细列表。
*   实现系统默认分类（大类+小类）。

### Phase 2: 核心功能强化
*   支持转账、借贷类型的账单录入。
*   支持自定义分类及分类排序。
*   引入多账本体系。
*   实现月度“大分类”预算及总预算设置、首页进度条 UI 展示。

### Phase 3: 体验打磨与高级功能
*   完善离线缓存策略，实现弱网/无网无缝切换。
*   上线 CSV 数据导入及导出功能。
*   暗黑模式适配及 UI 细节优化（微动效）。
*   全端（Web / App / PC）打包自动化构建 (CI/CD via Github Actions & CF Pages)。

---

## 8. 项目经理寄语 (PM's Note)

各位开发同仁：
我们正在打造一款可能陪伴用户数年甚至数十年的基础工具软件。“**慢即是快，稳即是赢**”。
在代码实现上，请务必关注 **冷启动速度** 和 **防误触设计**。功能可以精简，但绝不妥协交互的流畅度与数据的安全性。请随时在我们的协作工具上反馈 D1 数据库或前端打包过程中遇到的阻碍，我们将及时调整技术细则。

祝我们合作顺利，按时交付高质量产品！